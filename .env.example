# App env (no secrets). For local dev ONLY.
# Use disable/disable for local Postgres to avoid SSL mismatch headaches.
POSTGRES_URL="postgres://admin:password@127.0.0.1:5432/eventicro?sslmode=disable"
PGSSLMODE=disable

# Upstream service URLs (public or tailnet), adjust to your setup:
APP_BASE_URL="https://app.eventicro.hr"
API_BASE_URL="https://api.eventicro.hr"
TILESERVER_URL="https://tiles.eventicro.hr"
# Optional:
# SENTRY_DSN=""
