import { sveltekit } from '@sveltejs/kit/vite';
import { defineConfig } from 'vite';

export default defineConfig({
	plugins: [sveltekit()]
});
import { defineConfig } from "vite";
export default defineConfig({ ssr: { external: ["pg"] } });
